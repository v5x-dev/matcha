/** longest single chat message. shared so the textarea and the server agree on the cutoff. */
export const MAX_MESSAGE_LENGTH = 500;

/** newest messages the chat panel keeps in memory for one match. */
export const MESSAGE_LIMIT = 200;
